import { Canvas, Meta, Story } from '@storybook/blocks'
import * as EmbedStories from './Embed.stories'

<Meta of={EmbedStories} />

# Embed

Use any generic HTML embed code from third-party services.

<Canvas>
  <Story of={EmbedStories.Default} />
</Canvas>
